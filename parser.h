#ifndef PARSER_H
#define PARSER_H

#include <stdio.h>
#include <stlib.h>
#include <gumbo.h>

void recursive_tree_navigation(GumboNode *node, Queue *queue) {
	if(node.type == GUMBO_NODE_ELEMENT){

	}

}





Queue* find_links(const char *html_buffer, const char *base_url) {
	const char *root = gumbo_parse(html_buffer);

}










#endif
